import React from 'react';

const Reservations = () => (
   <div className="premium-card">
    <h2>My Reservations</h2>
    <p style={{ color: 'var(--text-muted)' }}>You have no active reservations at the moment.</p>
  </div>
);

export default Reservations;
